#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints the low case alphabet in reverse,
 * followed by a new line
 *
 * Return:always 0(success)
 */
int main(void)
{
	int al;

	for (al = 'z'; al >= 'a'; al--)
	{

#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints the low case alphabet in reverse,
 * followed by a new line
 *
 * Return: 0 if success
 */
int main(void)
{
	int al;

	for (al = 'z'; al >= 'a'; al--)
	{
	putchar(al);
	}
	putchar(10);

	return (0);
}
