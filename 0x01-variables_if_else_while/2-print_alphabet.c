#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Prints the alphabet in lowercase
*
* Return: 0 if success
*/
int main(void)
{
	char l = 'a';

	while (l <= 'z')
	{
	putchar(l);
	d++;
	}
	putchar('\n');
	return (0);
}
